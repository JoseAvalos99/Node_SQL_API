const config = {
    "user": "sa", //default is sa
    "password": "UTS12345.",
    "server": "localhost", // for local machine
    "database": "TYM", // name of database
    "options": {
        "encrypt": false,
        "enableArithAbort": false
    }
}
module.exports = config;